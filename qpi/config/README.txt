# Configuration templates

Can be selected in the drop-down menu on the start page.

The config.yaml files are equivalent to code in terms of security, so only trusted configs should be placed here.

Edit the loop in ../templates/index.html to add or remove templates